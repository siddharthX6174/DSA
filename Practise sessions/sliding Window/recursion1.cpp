#include<iostream>

/*
	OP : we want to build the building to;; the nth floor.

	BP : We want to build the building from 1st to nth flooe.
	SP: We want to build the building from 2nd to nth floor.

	Changing Paramete: One that represents from which building we are trying to
	biold till nth floor.

	F(1) : We wamt to build the building from 1st to nth floor.
	F(2) : We want to build the building from 2nd to nth floor.


	F(x) : We want to build the building from xth to nth floor,

	Recursibe Relation:


	F(n) : We are trying to buold floor from nth to nth.

	Recursion is a techniqye with fundamentals similar to loops:

*/

void F(int x) {

	/*

	*/

	// Stopping Point : Base Case:
	if (x == n) {
		// Thil will man we are triying to find the answer fo the state F(n) which the last
		// state and hence we know its answer.

		// I will return to give the answer for the current state.
		// Return main usko karunga jisne mughe call kiya hain.

		return (Build the nth floor);
	}

	// tASK
	bUILD Floot x.
}


int main() {

}